// ----------------------------------------------------------------------

const Logo = ({ sx = "h-10 w-10 lg:h-20 lg:w-20" }) => {
  return (
    <img src="/logo/logo.svg" alt="Logo - Moving Calculator" className={sx} />
  );
};

export default Logo;
