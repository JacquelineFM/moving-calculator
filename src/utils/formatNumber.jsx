// ----------------------------------------------------------------------

/**
 * It takes a number and returns a string with the number formatted with commas
 */
export const formatNumber = (number) => number.toLocaleString("es-MX");
